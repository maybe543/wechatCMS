$(document).ready(function() {
    var a = $(window).height();
    $("img.lazy").lazyload({
        effect: "fadeIn",
        threshold: a
    });
    if ($("#go_top")) {
        $("#go_top").find("em").each(function(){
            if ($(this).text() > 0) $(this).show();
        });
        0 < $("#go_top").size() && $(function() {
            $(window).scroll(function() {
                (300 < $(this).scrollTop()) ? $("#go_top a#a_top").css("display","block") : $("#go_top a#a_top").css("display","none")
            });
            $("#go_top a#a_top").click(function() {
                $("body,html").animate({
                        scrollTop: 0
                    },
                    800);
                return ! 1
            })
        });
    }
});
function pad_zero(a, c) {
    return a.toString().length >= c ? a: pad_zero("0" + a, c)
}
function getCookie(a) {
    var c = null;
    document.cookie && (a = document.cookie.split(escape(a) + "="), 2 <= a.length && (c = a[1].split(";"), c = unescape(c[0])));
    return c
}
function trim(a) {
    return a.replace(/^\s+|\s+$/g, "")
}
function validate_email(a) {
    regex = /^[^\s]+@[^\s]+\.[^\s]{2,3}$/;
    return regex.test(a)
}
function isiPhone() {
    return - 1 != navigator.platform.indexOf("iPhone") || -1 != navigator.platform.indexOf("iPod")
};